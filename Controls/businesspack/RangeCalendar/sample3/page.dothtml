<bp:RangeCalendar SelectedStartDate="{value: StartDate}"
                  SelectedEndDate="{value: EndDate}"
                  Mode="Date" />

<bp:RangeCalendar SelectedStartDate="{value: StartDate}"
                  SelectedEndDate="{value: EndDate}"
                  Mode="Time" />

<bp:RangeCalendar SelectedStartDate="{value: StartDate}"
                  SelectedEndDate="{value: EndDate}"
                  Mode="DateTime" />